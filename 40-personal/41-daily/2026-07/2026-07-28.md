# 2026-07-28

## Done
- [x] 렌트 홈페이지 MaaS 문의 모달 팝업 구현 + 상세상품 선택기 (8카테고리 18개 상품)
- [x] Cloudflare Pages 배포 2회 (모달 구현 → 레이아웃 수정)
- [x] 데스크톱+모바일 Playwright 검증 완료

### AX 교육 강의자료 — "대기업을 나와 무엇이 두려웠을까"
- **designer-value-lecture-slides.html** 30장 완전 새로 제작 (기존 "AI시대 디자이너가 디자인해야 할 것" → 대표 실제 경로 중심 내러티브로 전환)
- 7챕터 구성: 오프닝 → 대기업은 최고의 학교 → 왜 나왔을까 → 두려움 → 현실(10%) → 평가기준 변화 → AI시대 가능성 → 각오 → 클로징
- **designer-value-lecture-slides.pptx** 편집 가능한 PPTX 생성 (pptxgenjs)
- DDP 영디자이너 강의 PDF 디자인 참고 → 미니멀 리디자인 (다크+화이트+퍼플 3종 배경, 카드/장식 제거, 큰 타이포+여백)
- 폰트 Pretendard 전체 통일, 사이즈 7단계 상수화(52/44/42/38/34/20/18pt), 줄간 3종(1.4/1.5/1.6) 통일
- 경로: `10-projects/28-ai-education-ax/05-lectures/`

### 서울디자인어워드 2026 1차심사 — 122건 완주 (마감 당일)
- **출품작 설명 수집** — Playwright 로그인 후 페이지 내 fetch로 122건 상세 병렬 수집. 누락 0
- **한글 번역** — 본문 47만자 + 이미지캡션 5.7만자, 8건씩 16배치. 원문 대비 항목 누락 자동 검증 통과
- **채점 122건** — 5개 항목 × 10~20점. 1차에서 Very Good이 52.5%로 과다해 앵커 보정 1회(15·16점 하향) → Excellence 3 / Very Good 37 / Good 74 / Satisfactory 7 / Fail 1, 평균 77.0
- **사이트 점수 입력** — 드롭다운 610개를 클릭하는 대신 autosave API 직접 호출. 122건 전부 `Complete`, 개별 재조회 대조로 불일치 0
- **영문 코멘트 40건 입력** — 80점 이상 필수 규칙 충족. `POST /comment` (점수와 별개 경로). 병렬 시 CSRF 회전으로 401 발생 → 순차+재시도로 전환해 전건 성공
- **/문체 스킬 적용** — 영문 필수라 한자·사자성어는 불가, 이전 가능한 DNA만 적용(단문 단언↔장문 부연 교차, "A가 아니라 B" 재정의, 귀납, 헤지 없는 단정조). AI 상투어 검출 0건
- **엑셀 6시트 + 재검토 HTML** — 영문 입력본 옆에 한글 대역 열 추가(문단 1:1 대응), 판단 갈릴 21건 별도 정리
- 경로: `10-projects/43-seoul-design-award-judging/`
- ⚠️ 남은 것: 점수·코멘트 최종 검토는 본인 몫. 80점 경계 11건과 제출 이상 6건이 판단 지점

### ARISA 포트폴리오 — 카드 유실 2건 복구 + 재발 방지
- **병원프로젝트 유실** — 대시보드에서 안 보이던 원인은 삭제가 아니라 **맥미니 배포 미반영**. 로컬은 브리프 18필드·태스크 25 온전, 맥미니는 7/16 백필 원본(1.6KB)이었다. 재배포 후 정상. 현대차 미반영 1건도 함께 배포
  - 배포 과정에서 내가 만든 손실도 발견·복원 — 로컬본으로 덮어쓰며 맥미니의 `Done/100` 2건이 되돌아갔다. **태스크 목록만 대조하고 status·progress를 안 봤던 게 원인**
- **중기 팝업스토어 유실** — 7/18 중복 정리(껍데기 `중기제품팝업스토-a8ay6` 삭제 → `중기-팝업스토어-jufws`에 별칭 추가)까지는 정상이었고, 그 뒤 파일째 사라짐. **삭제는 로그를 안 남겨 시점 특정 불가**, git 미추적 + `/tmp` 백업 소실로 복구 사본 없음
  - 7/24 주간리포트 잔존 정보로 재등록(PM 배성원·~12/31·진척 70%). goal·client·KPI는 지어내지 않고 공란 + 이슈로 남김. 업무 이력은 복구 불가
- **재발 방지 3종**
  - 삭제 시 `projects/_trash/{id}.{시각}.json` 스냅샷 + `archive-log`에 `action=delete` 기록(PM·태스크수·삭제된 분장수까지)
  - 프로젝트 데이터 21개 전량 git 커밋 — 그동안 미추적이라 과거 스냅샷이 없었다
  - `portfolio-register.py`에 `aliases` 병합 지원 — 표기 변형이 카드로 매칭되는 근거라 재등록 시 함께 복원되어야 함
- **담당자 보완 요청 발송** — 브리프가 빈 카드 15건을 PM별 1통으로 묶어 텔레그램 발송(전제훈4·윤혜정4·최원석5·배성원2+1). 카드별로 *무엇이 비었는지*와 *등록 업무 수*를 명시, "끝난 건이면 아카이브" 선택지 포함

### ARISA 기능 추가 — 외부 파트너 · 중단 아카이브 · 카드 UI
- **외부 파트너**: `partners[]` 신설, **이름·이메일 없으면 등록 중단**(경고 아님). 연락처 없는 파트너는 인수인계 때 추적 불가라서. CLI `--partner "이름=이메일=소속=역할"`, 화면 폼·검증도 같은 기준. `members`는 열람권한과 연결되므로 사내 전용 유지
  - `/api/project/save`가 통째 덮어쓰기라 폼이 모르는 `partners`가 저장 한 번에 사라지던 함정도 함께 막음
- **아카이브에 '중단·무산' 종료 유형**: 완료 전제(납품·정산)로만 설계돼 엎어진 건은 아카이브 자체가 불가능했다. `dropped`는 납품·정산 대신 **중단 사유 귀속(클라이언트/우리 측/외부) + 상세**를 강제. 면세처럼 "왜 엎어졌나"를 남겨야 회고가 학습이 된다. 마시로우가 첫 실사용
- **카드 UI 3건**: ①`desc` 비면 `brief.summary` 폴백(21개 중 13개가 "설명 없음"이었다) ②액션 배지 3개가 같은 좌표 절대배치라 겹쳐 뭉개지던 것 → 푸터를 문서 흐름으로(Playwright 실측: 수정 전 12/21 카드에서 11.5px 겹침 → 0px) ③파트너 표시

### 문서 시뮬레이터 — 편집 가능한 HTML 내려받기
- 회의분석 산출물을 `.doc`로 받아 워드에서 고쳐야 했다. **내려받는 HTML 자체에 편집 툴바를 심어** 브라우저에서 바로 고치고 재저장(툴바 유지 → 몇 번이든 재편집, 인쇄 시 자동 숨김)
- 재업로드는 뺐다 — 업로드 HTML을 대시보드 오리진에 서빙하면 stored XSS가 세션 권한으로 돈다. sanitizer·CSP·sandbox가 코드베이스에 전무해 무게 대비 과함. **편집기를 파일 안에 넣어** 서버 저장소·엔드포인트·보안처리 없이 해결(52줄)
- 중간에 `\n` 이스케이프가 JS 평가 시점에 실제 개행이 되어 `SyntaxError` — 브라우저로 실제 열어보지 않았으면 못 잡았을 버그

### HR 포털 — 세금계산서 VAT 별도 + 미결 건 상시 노출
- **세금계산서**: 공급가액(VAT 별도) 입력 + 세액·합계 자동 계산, **품목(상품명) 필수**, 과세유형 3종(과세10%/영세율/면세 — 면세는 "계산서로 발행" 경고). 세액은 서버 재계산, 조작된 `tax_type`은 과세로 폴백. 승인 화면·상세·카카오워크 알림에 전부 노출
- **사업자등록번호가 시트에 저장되지 않고 있었다** — 시트에 컬럼 자체가 없어 동기화가 조용히 버리고 있었음(발행 필수 정보인데). 시트 15→20열 확장하며 함께 복구, 기존 데이터 무이동 확인
- **"결제하면 적재가 안 된다" 조사** — 결제 탭은 스키마 완전 일치, 재시도 큐 0건, 로그 에러 0건. 실제 원인은 **목록이 최근 2개월만 표시**해 두 달 넘긴 미결 건이 화면에서 사라진 것 → 종결(승인·반려·송금완료 등)이 아닌 건은 월 무관 상시 노출로 변경. 결제뿐 아니라 휴가·야근·일용직·세금계산서 공통 적용
- 커밋 `c0979ae`(HR 레포, 맥미니). ⚠️ **push 미완** — 맥미니에 GitHub 인증이 없어 로컬 커밋 상태

## 배운 것
- **"배포했다 ≠ 반영됐다"가 또 나왔다.** 병원 건은 등록 스크립트를 안 거쳐 배포 단계가 실행되지 않았고, 백업 파일명 형식(`.bak-20260727` vs `.bak-20260727-2006`)이 그 증거였다. 등록은 반드시 `portfolio-register.py`로
- **덮어쓰기 전에 무엇이 사라지는지 먼저 본다.** 태스크 "목록"만 비교하고 상태값을 안 봐서 완료 표시를 되돌렸다. 상위집합 확인은 키뿐 아니라 필드 단위로
- **로그 없는 삭제는 조사 자체가 불가능하다.** 아카이브·병합은 추적됐지만 삭제만 흔적이 없어, 중기 팝업스토어는 "언제 왜"를 끝내 못 밝혔다
