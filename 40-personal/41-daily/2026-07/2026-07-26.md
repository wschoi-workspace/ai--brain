# 2026-07-26 (토)

## Done

### ARISA 정보 취합·분류 기준 정립 (맥킨지 × 6-Layer) → v1.1 + 운영 반영
"정보가 취합되지만 정리 못 되고 나열된다"는 문제를 기준 정립 + 코드 반영으로 해소.

- **문제 진단**: Engine D가 7범주 분류만 하고 범주 내 우선순위·상위 종합이 비어 `items`가 "LLM 응답=시트 행 순서"로 나열. 정렬은 대표창 렌더링에서만 부분 적용.
- **기준 정의서** `arisa-info-classification-criteria-v1.md/.html`: 맥킨지 방법론(MECE·Pyramid·Impact×Urgency·80/20·SCQA·Ghost Deck + v1.1 가설주도·RAPID) × Reporting OS 6-Layer 확장 매핑. §6에 검토 방법론 전체 채택/보류 기록(이슈트리·Driver·5Whys·7S 보류).
- **정렬** `sort_items()`: `(범주→긴급도→impact→미결경과)` 결정론적 키. build_brief_data·build_team_brief_data에서 소스부터 정렬 → 모든 소비자 공유. "취합=정렬완료".
- **가설 헤드라인(Day-1 Answer)**: headline을 반증 가능한 주장으로. A/B/C 3안 검증 결과 **현행이 최선**으로 판정(후보가 못 이김) → 변경 안 함.
- **결정권한(RAPID)**: decision에 recommender/decider. 정적 브리프HTML + **라이브 대표창(dashboard-server)**까지 노출. **결정권자 미상 시 프로젝트 PM 폴백**("(PM)" 마커). 실데이터에서 "추천 김도영 → 결정 배성원(PM)" 확인.
- **weekly `_open_decisions`** impact 순 정렬.

### 프로젝트 주간 써머리 (weekly 대시보드 확장)
- `aggregate_project()`: 프로젝트별 6섹션 — 금주 처리 / 예정 대비(완료·미완) / 차주 예정 / 리스크 / 의사결정구조(RAPID) / 주요 변동(status-log). 소스 전부 재사용(assignments·주간브리프·decisions.jsonl·status-log).
- **변동만 있는 프로젝트는 접이식**(`<details>`)으로 분리 — 실질 진행 프로젝트만 카드 노출.
- 이번주 실데이터 테스트(비파괴, 시트 무변경): 10개 중 펼침 4 + 접힘 6.
- **자동화**: 새 launchd `com.projectrent.weekly-project`(토 09:30 `--week this`) — 금주 마감 회고를 토 10시 전 생성. 기존 월 08:30(지난주)은 유지.

### 배포
- 커밋 다수(sort_items·criteria·RAPID·PM폴백·프로젝트써머리·접기), 맥미니 배포(daily-brief·weekly·dashboard-server) + 대시보드 재기동(HTTP 200) + 토요일 launchd 부트스트랩 완료.

### PROJECT RENT 홈페이지 문의 → Gmail 전달 체계 구축
- 문의 취합 시트 확인: 문의 2건 접수 확인 (홍지원/사회복지공동모금회, 박다은/마이스피플)
- 기존 2건 ws.choi@project-rent.com으로 메일 전달 완료
- Apps Script 웹훅에 GmailApp.sendEmail 추가 (reply-to=문의자 이메일 → 답장 시 바로 회신)
- 새 Apps Script 배포 + Cloudflare SHEET_WEBHOOK_URL 시크릿 업데이트 + Pages 재배포
- 테스트 문의 → 시트 기록 + 텔레그램 알림 + Gmail 수신 모두 정상 확인
- 박다은/마이스피플 건은 7/31까지 회신 요청 — 회신 필요

### AX 교육 강의자료 — 기획자 버전 신규 + v2 디자인 통일
- **기획자-프레젠테이션-v1.html** 신규 제작 (24장): "기획자는 왜 AI를 잘 써야 하는가" — 5가지 이유(과정 압축/사고 확장/반복 가속/설득 번역/범위 확대) + 실전 5단계
- **디자인컨설팅-프레젠테이션-v2.html** 동일 디자인 가이드 적용: 폰트 30% 확대 + S27 헤더 "기획자의"로 변경
- 두 파일 모두 배경 완전 블랙(#000) 통일, R스타일 가이드(Pretendard/#6C5CE7) 유지
- 경로: `Dropbox/02-프로젝트렌트RENT/07.AI연습/이림 _강의2026/교육메뉴얼/`

## Notes
- 맥미니 배포는 scp 방식(레포 분기: 로컬 vs 맥미니 origin 상이). 배치 스크립트는 재기동 불필요, 대시보드 서버·launchd는 kickstart/bootstrap 필요.
- 과거 날짜 브리프 재생성 금지 — 원본 보고가 시트 윈도우 만료로 복구 불가(파생물 덮어씀).
- weekly 전체 배치는 `update_assignment_status_in_sheet` 시트 쓰기 부작용 有 → 테스트 시 no-op 몽키패치로 비파괴.
- RAPID 실사용: 대부분 "보고자 → PM" 기본값(명시적 결정권자 근거 드묾). 의도된 fidelity 동작.
- 남은 후보: 6-Layer SSOT 역참조 링크(문서위생), v1.2 이슈트리·Driver Tree.

---

## 노션 업무관리 시스템 분석 → ARISA 갭 해소 (저녁~새벽, 배포 완료)

### 1단계 — 노션 템플릿(에그디자인 무료 배포) 분석
- app.notion.com 비공개 페이지 → integration "클로드코드"에 공유받아 API로 덤프
- 구조: DB 3종(할 일/회의/프로젝트) + dual relation + 진행률 롤업 + '오늘 할 일' 체크박스. 7/18 1차 갭분석에서 대부분 흡수 완료 상태였음
- **실질 갭 3건 도출 → 전부 구현·배포**
  - **갭A 회의↔할 일 연결**: 회의 ID를 새로 만들지 않고 프로젝트 문서함 `(pid, ts)` 재사용. 시트 M열 출처·N열 출처ID + `/api/meeting-actions` + 회의 제출 직후 등록 UI + 문서함 실행률 배지(`실행 3/7 · 43%`)
  - **갭B 출처 일반화**: 어휘 6종(회의·일일보고·주간계획·대표지시·리더분장·본인등록), 등록 경로 5개 전부 배선, 리더홈 유입 분포
  - **갭C 오늘 선언**: `shared/today_plan.py` — 담당자가 ☀️로 오늘 할 것을 직접 고르고 계획 대비 실행률 집계. 리더홈에 팀원별 진행바

### 2단계 — 노션 구축 상세가이드(10~50인) 대조 + 실측
- 리포트: `20-operations/23-arisa/notion-guide-comparison-2026-07-26.html` (5축 판정 + 실측 근거 + 우선순위)
- **실측이 드러낸 것**: 열린 80건 중 **미착수 78·지연 47** / 마감 없음 24%·프로젝트 미연결 17%·**필수4종 누락 38%** / **결과물 G열 100% 공란** / 우선순위 '긴급' 6% / 프로젝트 21개 중 docs 0건
- **핵심 발견**: 보고→상태 환류가 *이미 있었으나* 문이 너무 좁았다. PERSON_BRIEF_PROMPT가 "진행 중·예정·부분 완료 절대 금지"로 명시적 완료만 추출 → **개인 브리프 57개에서 completed 2건**(같은 브리프 new_todos는 142건). '진행중' 전이는 설계에 아예 부재
- ⚠️ 세션 중 자기 오보고 정정: status_log 집계 키를 `actor`로 잘못 조회 → 실제 키는 `by` (전이 23건 = 자동 8·사람 14·테스트 1). 리포트 5곳 정정 반영

### 3단계 — 우선순위 1~4 순서대로 전부 구현·배포
1. **보고→상태 환류**: 보고 직후 봇이 LLM 매칭 결과를 [▶진행중][✓완료][✕아님] 버튼으로 되묻기(1탭 확정) + 아침 자동 전이에 '진행중' 추가(미착수에서만). 안전장치 = callback_data에 업무 지문 6자 + `verify_row` 담당자·지문 재확인
2. **입력 완결성 게이트**: `incomplete()`(마감 없는 건 우선) → 리더홈·개인탭 "⚠️ 입력 누락" 섹션 + 등록 시 마감 미입력 차단. 실데이터 32건 즉시 감지
3. **결과물 칸 되살리기**: 사람에게 다시 묻지 않고 **일일보고의 산출물을 완료 확정 시점에 G열로 이관**
4. **업무 유형(O열) + 마감 달력**: `TASK_TYPES` SSOT, `_llm_todo`가 등록 시 자동 추정(입력부담 0), 리더홈 유형 분포 % + 2개월 마감 달력(몰림 농도)

### 구조 정리
- `shared/assign_sheet.py` 신설 — 주간분장 컬럼·파싱·상태갱신 SSOT. dashboard-server `_assign_read`를 이 모듈로 교체하고 봇도 동일 모듈 사용(세 번째 사본 방지, 실데이터로 서버·봇 결과 일치 확인)
- 시트 최종 스키마 **A~O 15칸**. 구행(12·14칸)은 빈값 하위호환

### 검증·배포
- 커밋 5건: `f640baa`(갭A) `7763dc3`(갭B·C) `82f362f`(환류) `eae31b4`(완결성) `d9a3e68`(결과물·유형·달력)
- 신규 테스트 6스위트(유닛 115+·E2E 66+) 전부 통과, 매 단계 실데이터 회귀 129행·열린 80·지연 47 불변
- 맥미니 배포 5회 전부 해시 대조→백업→scp→py_compile→kickstart→200 확인. 봇 재시작 3회(유휴 확인 후). 마지막 배포 7/27 07:04, `/tmp/pr-dashboard.err` 신규 에러 없음(최종 기록 7/22자 유지)

### 내일 관찰 포인트
- 저녁 보고 1건이 들어오면 세 가지가 한 번에 확인됨: ①LLM이 보고 문장↔분장을 실제로 붙이는지 ②완료 시 결과물이 옮겨 적히는지 ③아침 브리프가 '진행중' 전이를 몇 건 잡는지
- 전이 로그에서 `source: report-sync`(봇 버튼) vs `daily-brief-auto`(아침 자동)로 구분 확인
- 유형 분포·결과물은 신규 등록·완료분부터 채워짐 (현재 미분류 94·결과물 0은 정상)
