# 2026-07-06 (월)

> 맥미니 원격 운영 세션 — Second Brain 메모리 복구 + 대시보드 리더 권한 기능 + gws 인증 장애 진단/복구.

## Done

- [인프라] **맥미니 원격 접속 복구** — 외부망이라 LAN(192.168.219.249) 타임아웃 → Tailscale(`macmini-ts`)로 SSH+VNC 정상. 화면 공유(vnc://) 재오픈.

- [ARISA] **Second Brain 메모리 기능 복구** — 7/1 이관 후 3일간 죽어있던 mem0 적재·재부상 원인 발견·해결
  - 근본원인: `arisa_memory.py`가 맥미니에 없어 봇이 try-import 실패 시 조용히 메모리만 끄고 동작
  - **리포트 지정경로(`00-system/02-scripts/`) 오답 발견** — bot.py의 `import arisa_memory`(35행)가 `sys.path.insert`(56행)보다 먼저 실행 → bot.py와 같은 `24-second-brain/`에 배치해야 import 성공. scp 전송 → import OK → 봇 재시작 복구

- [대시보드] **신규 직원 3명 등록** (김예진·김도영·배성원, pin 0000, 개인별 수정가능) — `_data/users.json` + `portfolio-users.json`(재생성 소스) 반영, 로그인 API 실검증

- [대시보드] **리더 프로젝트 권한 배치 기능 구현** (커밋 `d6c71e1`)
  - 리더·대표가 직원 프로젝트 권한(멤버=열람) 배정 + 신규 생성 가능. 범위=**본인 팀 프로젝트만**(사용자 결정)
  - 서버: `can_manage`(대표·PM·본인 리더팀∩프로젝트팀), `/api/project/members` 신규, 생성 게이트 대표→대표·리더, canManage/canCreate 플래그
  - 클라: 카드 멤버목록 + 👥 멤버 배정 모달, 리더 추가버튼 노출
  - 검증: 권한 매트릭스 API 전수 통과(팀범위·차단·생성·실데이터 무결성), 브라우저 UI는 미검증

- [인프라] **gws 인증 장애 진단** — 7/4 20시 일일보고 리마인더 실패(텔레그램 경고)의 근본원인 규명
  - 이관 후유증: `credentials.enc`가 구 노트북 keyring 암호화라 맥미니서 복호화 불가 + launchd headless에서 OS keyring 접근 불가 + `KEYRING_BACKEND` 미설정
  - 복구설정: `~/.zprofile` + plist 4개(daily-report-bot/reminder/reconcile/report-backfill)에 `GOOGLE_WORKSPACE_CLI_KEYRING_BACKEND=file` 적용·리로드(봇 env 실측 확인)

## 미결 / 다음 세션

- 🔴 **gws 재로그인 (사용자 전용)** — 맥미니 데스크톱 터미널에서 `export GOOGLE_WORKSPACE_CLI_KEYRING_BACKEND=file; gws auth login --full` (브라우저 OAuth). gws는 브라우저 플로우만 지원 → 화면 공유 경유 필수. **완료 전까지 일일보고 시트 읽기·미보고자 DM·reconcile 중단 상태**
  - 로그인 직후: 시트 읽기 복구 확인 + `daily-report-reconcile` 수동실행으로 7/4 놓친 미보고자 대사
- 🟡 **대시보드 리더 권한 브라우저 UI 확인** — 전제훈(pin 3333) 로그인 → 카드 👥 멤버 배정 모달 동작 육안 확인
- 🟡 **basket 계열 plist gws backend** — basket-ops-bot/basket-weekly-insight도 동일 file 백엔드 설정 필요 시 적용
- 🟢 커밋 `d6c71e1` 맥미니 로컬 — 원격 push 여부 판단
