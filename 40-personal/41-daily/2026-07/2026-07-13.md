# 2026-07-13 (일)

## ARISA OS — 분장 위계 완성 + 리더 온보딩 개시

### 완료
- **분장 위계 검토·보완**: 대표→리더→팀원 위계는 기구현 확인. 리더 팀 홈에 "📥 받은 업무(대표 지시)" 분리 + [→ 팀원에게 상세 분장] 프리필 버튼 (b9a3e32)
- **대표창 지연·결재 섹션**: `/api/exec-attn` — 마감 경과 미완료 분장(D+N) + 미해결 결정 자동 집계 (7d4d612)
- **주간업무계획 업로드→분장 검토 초안**: 주간회의 엑셀 업로드 → 파싱 → AI 항목화 → 그룹 에디터 프리필. 실파일 13행→24건 검증 (c6baa6d, 맥미니 venv에 openpyxl 설치)
- **리더 분장 전체 반영**: 팀 브리프 우선파악존 + 프로젝트 상세 "분장 업무(2주)" 자동 섹션 (b460e11)
- **사용 가이드 배포**: arisa-os.com/guide-os (역할별/분장 사용법/FAQ) + /guide-flow 구조도 + 템플릿 xlsx 다운로드, 셸 '가이드' 링크 (2650ee0)
- **리더 온보딩**: 3인에게 ① 계정+가이드 ② 사용가이드 링크 개별 DM 발송 (대표 직접 실행 스크립트)
- 계정 점검: 11명 전원 강PIN, SSOT symlink 정상

### 결정·교훈
- 에이전트의 대외 발송·자격증명 취급은 권한 계층이 일관 차단 → **스크립트 준비 → 대표 `!` 직접 실행** 패턴 확립
- 포트폴리오 HTML 수정 시 원형(prototype)+생성본 동시 패치 필수

### 보고 시뮬레이터 — AI 드래프트 비교 기능
- **AI 비교 모드 구현 + 맥미니 배포 완료**: 직원이 먼저 폼 작성 → 자유 텍스트로 AI 드래프트 생성 → 필드별 비교·선택 교체
  - `SIMULATOR_DRAFT_PROMPT` + `POST /api/simulator/draft` + 접기 UI + 필드별 "이 값으로 교체"
  - `_call_claude` → `_call_llm_json` (Anthropic 우선 → OpenAI GPT-4o fallback), Python 3.9 호환 수정
  - E2E 검증 통과 (맥미니 실API, 비활성화→활성화→비교→교체→리뷰 전체 흐름)

### 내일/다음
- [ ] **대시보드 "이번 주" 탭 팀별 표시 + 업무 관리 별도 페이지** — 요청했다가 arisa-os 현재 페이지 기반으로 재정의하기로 하고 세션 종료(미착수). 참고: 0e533bb(week-brief 일별스택+/manage)를 대표가 06-40 revert함(사유="기존 시스템 내 변경을 원했는데 별도 페이지를 만들어서"). 재개 시 기존 탭 구조 안에서 최소 변경 원칙.
- [ ] **주간분장 시트 헤더 정합** — 사용자가 시트 헤더를 재정의(날짜·프로젝트명·팀·담당자·업무·일정·결과물·상태·이해관계자·우선순위J). dashboard는 맞췄으나 `/주간분장`스킬·`/assign`봇·`weekly-report-aggregate.fetch_assignments`는 옛 헤더라 컬럼 어긋남 → 재매핑 필요 ([[project_weekly_assignment]])
- [ ] send-leader-guide.py 삭제 (PIN 포함): `rm 20-operations/23-arisa/send-leader-guide.py`
- [ ] 직원 전체 공지 (새 주소 + 가이드 + 오늘 Brief 탭 신설)
- [ ] 월요일 아침 브리프 — 새 링크(arisa-os.com)로 첫 정기 발송 확인
- [ ] 리더 실사용 피드백 수집
- [ ] Anthropic API 키 갱신 시 `_call_llm_json` 자동 전환 확인

상세: `20-operations/23-arisa/23-progress.md`
