# 2026-09-16 - Daily Note

> 날짜: 2026-09-16
> 요일: 화요일

---

## 📋 오늘 한 일

### 워크스페이스 전수 진단 & 정리 (4.3GB → 2.7GB)

**① 세 갈래로 동시에 훑었다**

구조·용량 / 프로젝트 현황 / 스킬·git 위생을 병렬 탐색.
프로젝트 31 · 운영 9 · 스킬 67 전량이 대상이었다.
**탐색 결과를 그대로 믿지 않고 중요한 주장은 읽기 전용 명령으로 다시 확인**했는데, 두 건이 틀렸다.

| 보고된 내용 | 실제 |
|---|---|
| `.emok_reply.eml` 노출 | **이미 gitignore 등록됨** — 노출 아님 |
| 워크트리 3개 삭제 가능 | **미머지 커밋 24·5·1개 보유** — 단순 삭제 불가 |

두 번째를 그냥 믿었으면 arisa-vnext의 24커밋이 날아갈 뻔했다.

**② 진짜 위험은 용량이 아니라 "없어지면 못 되살리는 것"이었다**

| # | 위험 | 상태 |
|---|---|---|
| R1 | **유일본 스킬 5개가 git에 한 번도 안 들어감** — perspective(30KB)·매트릭스·전략레포트·혁신전략레포트·kakaotalk | 29파일 편입 완료 |
| R2 | `.claude/worktrees/` **1.7GB가 gitignore 미등록** → `add -A` 한 번이면 통째로 커밋 | 차단 |
| R3 | `.morning-tg.json/.py/.txt` 노출 — 언더스코어판 `.morning_tg.json`만 막혀 있었다. **표기 한 글자 차이로 방어가 빗나감** | 차단 |
| R4 | 미커밋 스킬 12건(gov-proposal +94, print-version +129) · origin 미push 17커밋 | 반영·push |

**③ 아카이브 이동이 gitignore를 무력화했다 — 오늘 가장 큰 함정**

`git rm --cached`로 파생물 303MB를 해제한 **직후** 프로젝트 6건을 90-archive로 옮겼더니,
`.gitignore`가 `/10-projects/...` 절대경로로 박혀 있어서 패턴이 빗나갔다.
staged가 **18.9MB여야 할 것이 181.9MB**로 튀었다.

커밋 전에 크기를 재보지 않았으면 방금 뺀 크롤 데이터 163MB가 그대로 다시 들어갔을 것이다.
→ `git reset` 후 새 경로로 패턴 갱신. 메모리 `feedback_archive_move_gitignore`에 기록.

**④ `rm`이 막혀 있어서 삭제 대신 격리로 바꿨다**

환경에서 `rm`이 차단돼 있었다. `mv`는 되길래 `90-archive/_cleanup-20260916/`(42MB)로 옮겼다.
결과적으로 **되돌릴 수 있게 된 셈**이라 이 방식이 더 나았다.

**⑤ 90일 넘게 방치된 프로젝트는 0건이었다**

문제는 방치가 아니라 다른 데 있었다.

- **시한 경과 4건** — 41-hospital-govt-rnd(시한 5건 경과), 36-R-Europe(액션 9건 전부 미착수·D-15),
  45-lotte(무응답), 46-seongsu(9/15 회신일 도래)
- **사람 손 한 번이면 풀리는 블로커 3건** — 26-card-news(Canva 자격증명, Cloudflare 봇차단으로 자동화 불가),
  47-sk-ai-native(편집모드 OFF 사본), 48-seongsu-popup-direction(대표 답변 3개)

**⑥ 스킬 규약 하나가 공회전 중이었다**

CLAUDE.md는 "어떤 스킬을 소환하든 ⚠️ 잔여작업 블록을 먼저 확인"을 필수로 걸어놨는데,
실제 그 블록을 가진 스킬은 **72종 중 `매트릭스` 1개뿐**이었다. 나머지는 항상 빈손으로 끝난다.
→ 규약에 현황을 명시하고, "미결이 실제로 생겼을 때만 블록을 만든다"로 범위를 정리했다.

---

## 📦 산출물

- `00-system/07-audit/2026-09-16-workspace-audit.html` — **진단 대시보드**(R다크, 7섹션). 실행 결과 배너 포함
- `00-system/07-audit/2026-09-16-cleanup-targets.txt` — 정리 대상 목록(삭제 전 기록)
- **progress.md 9건 소급 작성** — 38-ax-pre-diagnosis / 41-chungdam-nine-sns / 24-second-brain /
  25-basket-ops-manual / 26-reporting-os / 27-team-ops-guide / 29-transcript-seongsu(종결) /
  30-basket-report-webapp(종결) / 40-innisfree-sns(종결)
- 오번호 정정: `44-maas-marketing/02-us-la-playbook/34-progress.md` → `02-us-la-playbook-progress.md`
- **CLAUDE.md 갱신** — 스킬 등재율 45% → 전량(72종), 진행 프로젝트 현행화, ⚠️규약 현실화
- 커밋 7건 전부 push 완료

### 수치

| | 전 | 후 |
|---|---|---|
| 워크스페이스 | 4.3GB | **2.7GB** (+ 외부 잔재 1.67GB 별도) |
| 활성 프로젝트 | 31 | **25** |
| 워크트리 | 6개 | **1개** |
| 미push 커밋 | 17 | **0** |
| git 미등록 스킬 | 5 | **0** |
| progress.md 누락 | 9 | **0** |
| 2MB+ 추적 파일 | 36 | 14 |

아카이브 6건: 37-hyundai-rxr-cabin-air(42로 승계) · 40-innisfree-sns · 29-transcript-seongsu ·
30-basket-report-webapp · 29-xescmenzl-sns(중단) · 37-hangeul-culture-experience(중단)

---

## 🔜 남은 것

**바로 할 수 있는 것**
- **격리 폴더 실제 삭제** — `rm -rf 90-archive/_cleanup-20260916` (42MB, 내용 확인 후)
- `node_modules` 810MB — 특히 `38-ax-pre-diagnosis`는 469MB인데 소스가 164K

**정리 중 새로 발견 — 확인 필요**
- ⚠️ **`90-archive/30-basket-report-webapp/SETUP.md`에 PIN·관리자 비밀번호 평문** — git 히스토리에도 남아 있다.
  서비스가 살아 있으면 자격증명 교체 필요
- ⚠️ **`25-basket-ops-manual` launchd가 헛돌고 있다** — 온보딩 8회 발송을 끝낸 뒤에도 09-15까지 매일 실행
- **`41-chungdam-nine-sns` git 추적 0건** — 09-09에 이것 때문에 "자료 없음" 오판이 실제로 났던 폴더.
  progress.md만 추적에 넣었다. 나머지는 추적할지 / SSOT를 80-r-tech로 못 박을지 결정 필요

**보류한 것**
- **번호 충돌 38·41·43 세 쌍** — 아카이브로 29·37은 해소. 나머지는 참조 경로가 깨질 수 있어 보류
- **`.git` 329MB** — 추적은 해제했으나 히스토리는 남는다. `filter-repo`는 병렬 세션 위험으로 보류
- **미커밋 277건** — 데일리 139건 등 다른 세션·직접 작업분이라 손대지 않음
- **`매트릭스` ⚠️ 미결 3건** (2026-07-31 등록, 6주 반 경과) — E열 기본 정렬 / D↔D-1 짝 정렬 /
  예고 확장 3종(Target 해석·컨셉 해석 열·D-1 매출 산식). **렌더러 공통이라 새 프로젝트에도 그대로 걸린다**
