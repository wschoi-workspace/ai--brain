# 2026-08-09

## Done

### ARISA vNext P2 — WS4 제출 버튼 + WS6 Context Delta 층 (배포 완료, `727c057`)
- **선행 정리** — 일주일 사이 벌어진 히스토리 엉킴 해소. 맥미니 로컬 커밋(4eb7566)과 origin/main의 0240d18이 **patch-id 동일**(같은 수정이 두 경로로 커밋됨)임을 확인하고 origin/main을 브랜치에 병합, 맥미니는 `checkout -B main origin/arisa-vnext`로 재포인팅(내용 무손실 검증 후)
- **WS4** — R4 결과 화면에 "프로젝트로 제출" 버튼. 대시보드 프록시 경유(`API==='/r4'` 재작성 감지)에서만 노출, fetch는 프리픽스 없이 루트로 나가 8780 세션 쿠키 인증. projectMatch 제안 프리셀렉트(확정은 사람 클릭). Exec 탭에 역할별 라우팅(대표/리더/실무) 표시
- **WS6** — 회의가 프로젝트 상태를 갱신하는 층(개념 모델의 심장):
  - `shared/meeting_delta.py`: R4 결과 → delta **결정론 투영**(LLM 0회). 액션·산출물은 개수만 — 문서함이 SSOT
  - `shared/project_context.py`: 전 항목 since(meeting_ref) + changedBy 개정 연대기 + revert_ref, **appliedRefs+전사지문 2중 멱등**
  - 저장 `_data/project-context/{pid}/` (projects/*.json 금지 — git tracked라 회의마다 충돌 diff)
  - r4-import 성공 시 백그라운드 적용, `GET /api/project/context` 조회
- **검증** — 신규 테스트 21종(멱등·개정 페어링·revert) + 기존 142종 회귀 없음. 실세션(아랑재) E2E: delta 변경4·결정8·리스크5·미결7 정상 투영, **다른 세션 ID+같은 전사 재적용이 지문 가드에 차단됨을 실데이터로 확인**(arisa2 delta 중복 사고의 재발 방지 실증)
- 맥미니 8780·8781 재기동, health 200 · 제출 UI 서빙 · context 라우트 게이트 정상

### ⚠️ 미결
- **PR #2(arisa-vnext) GitHub 미머지** — main은 16e2d41에 멈춰 있고 운영은 브랜치 배포(무영향). 기록 정합 위해 머지 필요
- 구세션 delta는 scope/why가 빈 값(정상) — 새 프롬프트로 분석하는 회의부터 채워짐
- WS7(브리프 delta 연동)·WS8(엔진 A 대체)·WS9(회차 비교) 대기
